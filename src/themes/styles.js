import { esES } from "@mui/material/locale"

const styles = (theme) => (
  {
    palette: {
      primary: { main: "#1976d2" },
    },
    // eslint-disable-next-line no-sequences
  },
  esES
)

export default styles
